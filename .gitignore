node_modules
dist
.vercel
.DS_Store

.env
.env.*
!.env.example

.captures

# fixtures/ holds only responses with no personal data. Player and battlelog
# captures stay local; generate your own with `npm run fixtures`.
fixtures/player.json
fixtures/player-battlelog.json
